[pytest]
asyncio_mode = auto
# Pinned explicitly: unset, pytest-asyncio warns and will switch this default to
# "function" in a future release. Our async fixtures are already function-scoped,
# so this preserves current behaviour rather than inheriting the change later.
asyncio_default_fixture_loop_scope = function
testpaths = tests
